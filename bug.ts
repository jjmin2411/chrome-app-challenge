/*
There is a bug where completed `<li>` elements disappear when the page is reloaded.However, the `<li>` elements in the todoList don't disappear even after reloading the page.
If Nico is reading this, I would appreciate it if you could leave a suggestion for revision or any comments.
*/
